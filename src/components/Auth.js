import React from 'react';

const Auth = (props) => {
function login(){
    
}

    return (

        <div>
            Auth Page 
            <button onClick={login}>Login</button>
        </div>
    );
}

export default Auth;
